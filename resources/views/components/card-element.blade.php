<div class="card">
    <div class="btn-group">
        <a class="btn btn btn-outline-dark" href="{{ route('add-balance') }}">Add Balance</a>
        <a class="btn btn btn-outline-dark" href="{{ route('payments') }}">Payment Method</a>
        <a class="btn btn btn-outline-dark" href="{{ route('users') }}">Fund Transfer</a>
        <a class="btn btn btn-outline-dark" href="{{ route('saved-card') }}">Saved Cards</a>
        <a class="btn btn btn-outline-dark" href="{{ route('bank-accounts') }}">Bank Accounts</a>
    </div>
</div>
<p></p>